import { Component } from '@angular/core';

@Component({
  selector: 'app-login',
  styleUrls: ['login.component.scss'],
  template: `
    <div>
      Login Component
    </div>
  `
})
export class LoginComponent {
  constructor() {
  }
}
